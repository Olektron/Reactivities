export interface IActivity {
   // map(arg0: (activity: any) => any): import("react").ReactNode;
    id: string;
    title: string;
    description: string;
    category: string;
    date: string;
    city: string;
    venue: string;
}